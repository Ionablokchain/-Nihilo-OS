# Nihilo OS – Tools

This directory contains helper utilities for developing and debugging Nihilo OS.

## `flux_format.py`
Formats `.flux` source code with consistent indentation.

```bash
python3 flux_format.py kernel/main.flux --output formatted.flux --indent 4